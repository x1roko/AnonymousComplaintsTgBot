# 🤖 Anonymous Telegram Bot (aiogram + MySQL)

Этот проект представляет собой минималистичный Telegram-бот, разработанный на фреймворке **aiogram** (Python), который анонимно записывает текст сообщений пользователей в удаленную базу данных **MySQL**. Развертывание осуществляется с помощью **Docker** и **Docker Compose**.

## 🚀 1. Развертывание и Запуск

Для запуска приложения необходимо иметь установленные **Docker** и **Docker Compose**.

### 1.1. Настройка Базы Данных (Предварительный шаг)

Предполагается, что ваша БД MySQL уже развернута и доступна по сети.

1.  **Создайте БД**: Используйте `schema.sql` для создания базы данных (`telegram_anon_db`) и таблицы `anonymous_messages`.
2.  **Создайте Пользователя**: Создайте пользователя с правами на **SELECT, INSERT, UPDATE, DELETE** для работы бота (например, `bot_user`).

### 1.2. Настройка Файла Секретов (`.env`)

Создайте файл `.env` в корне проекта и заполните его учетными данными.

```dotenv
# Конфигурация Telegram
BOT_TOKEN="ВАШ_ТОКЕН_БОТА_ИЗ_BOTFATHER"

# Конфигурация MySQL (Для пользователя 'bot_user')
DB_HOST="IP_ИЛИ_ДОМЕН_ВАШЕЙ_ВНЕШНЕЙ_БД"
DB_DATABASE="telegram_anon_db"
DB_USER="bot_user"
DB_PASSWORD="secure_password_bot"
DB_PORT=3306
```
## 🔑 Проверка Целостности Кода (Хэш)

Чтобы доказать, что исходный код проекта не был изменен, используется SHA-256 хэш, вычисленный от содержимого ключевых файлов проекта (кроме `.env`).
5dc11c678e8c3c5e5f297812bc0dd292323b49c4bdca218d704403ac218ec880

### Как проверить
Для проверки клонируйте проект и выполните следующую команду в корневой директории:

```bash
cat bot.py docker-compose.yml Dockerfile requirements.txt schema.sql | sha256sum | cut -d ' ' -f 1
